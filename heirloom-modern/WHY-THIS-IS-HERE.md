# This folder does not belong in the instantcma repo

It is staged here only because the session that built it could not create a new
repository — the GitHub App token was scoped to `behrandbehr-create/instantcma`
and `POST /user/repos` returned 403.

Heirloom Modern is a separate business from Instant CMA. See `README.md` in this
folder for the two-step move (create the org, create the repo, copy the folder in),
then delete this directory from `instantcma`.
